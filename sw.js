
self.addEventListener('activate', function(event){
    //fetch all currencies and add to indexedDB
    //app.js
    //materialize css and javascript
    //index.html
})

self.addEventListener('fetch', function(event){
    //if css, or js, send from cache, else if currency API
    //fetch and cache, else return cache
})